{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "migrate-to-firestore": "tsx ./src/migrate-to-firestore.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "pg": "^8"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "^8",
    "tsx": "catalog:"
  }
}
